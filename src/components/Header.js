import React from 'react';
import logo from '../images/ono.png';

const Header = () => {
    return (
        <header>
            <img src={logo} alt={logo} />
        </header>
    )
}

export default Header














